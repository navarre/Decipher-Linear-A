# Manifest — raw website import

## Snapshot summary

- Source directory: `minoan-linear-a/inbox/lineara website/`
- Destination directory: `minoan-linear-a/imported/lineara-website/site/`
- Import date: 2026-04-01

## Observed top-level structure

- HTML entry pages (`index.html`, `browse.html`, `map.html`, `about.html`, `help.html`, etc.)
- asset files (`style.css`, `style-paper.css`, `database.js`, `sigilWeb.bc.js`, images)
- subdirectories for content dimensions (`document/`, `kind/`, `location/`, `period/`)
- embedded paper assets (`paper.html`, `paper.pdf`)

## Notes

- This is treated as a raw source import.
- `.DS_Store` files are intentionally excluded from git.
- Further analysis/mapping should happen after the snapshot is preserved.
